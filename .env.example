# File Browser Configuration

# Port to listen on (Railway sets this automatically)
PORT=8080

# Root directory to serve
ROOT=/srv

# Default admin username (used on first run)
FB_USERNAME=admin

# Default admin password (plain text — hashed on first run)
FB_PASSWORD=admin

# Path to the database file
FB_DATABASE=/srv/filebrowser.db
